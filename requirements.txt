anthropic>=1.0.0
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
python-dotenv>=1.0.1
pypdf>=5.0.0
python-docx>=1.1.2
python-pptx>=1.0.2

# Donation QR codes, rendered locally so the help page works offline.
segno>=1.6.0

# Desktop app window (optional - falls back to the browser without it)
pywebview>=5.0
pillow>=10.0
